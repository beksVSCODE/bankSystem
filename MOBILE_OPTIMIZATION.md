# Мобильная оптимизация Dashboard

## Применённые улучшения

### 1. Вкладки фильтрации (Segmented компонент)

#### Адаптивность
- **Мобильные устройства (< 640px)**: Компонент использует эмодзи и сокращённые названия
  - 🏦 Все → "Все счета"
  - 📈 АУ → "Кабинет АУ по физлицам"
  - 💳 Карты → "Карты"
  - 👤 Личные → "Счета физ.лица"
  - 🏢 Бизнес → "Расчётные счета"

- **Горизонтальный скролл**: Контейнер имеет `overflow-x-auto` для удобства на мобильных
- **Динамический размер шрифта**: 
  - Desktop (> 640px): 14px
  - Tablet (< 640px): 12px
  - Mobile (< 480px): 10px

- **Полное название показывается под вкладками**: На мобильных устройствах под Segmented компонентом отображается полное название выбранной категории

#### CSS для Segmented
```css
/* Mobile responsive Segmented */
@media (max-width: 640px) {
  .custom-segmented-white {
    font-size: 11px !important;
  }
  .custom-segmented-white .ant-segmented-item {
    font-size: 11px !important;
    padding: 6px 8px !important;
  }
}

@media (max-width: 480px) {
  .custom-segmented-white {
    font-size: 10px !important;
  }
  .custom-segmented-white .ant-segmented-item {
    font-size: 10px !important;
    padding: 4px 6px !important;
  }
}
```

### 2. Общий баланс

#### Исправления
- **Размер шрифта**: Использует responsive sizes
  - Mobile: `text-xl` (1.25rem)
  - Extra Small: `xs:text-2xl` (1.5rem)
  - Small: `sm:text-3xl` (1.875rem)
  - Medium и выше: `md:text-4xl` (2.25rem)

- **Защита от переполнения**:
  - Добавлено `break-words` для предотвращения выхода за контейнер
  - Уменьшены отступы на мобильных: `mb-2 sm:mb-3` вместо `mb-3 sm:mb-4`

- **Улучшены интервалы**: 
  - Balance Card gap: `gap-4 sm:gap-6` вместо `gap-6`
  - Доходы/Расходы: `gap-3 xs:gap-4 sm:gap-6` вместо `gap-4 xs:gap-6`

### 3. Кнопки Quick Actions (Быстрые действия)

#### Адаптивность
- **Размеры на разных экранах**:
  - Mobile: `grid-cols-4 gap-1.5` с размером иконок `text-sm`
  - Extra Small: `gap-2` с размером иконок `xs:text-base`
  - Small и выше: `flex gap-3` с размером иконок `sm:text-xl`

- **Padding**:
  - Mobile: `p-1.5` 
  - Extra Small: `xs:p-2`
  - Small и выше: `sm:p-3`

- **Размер текста**:
  - Mobile: `text-[8px]`
  - Extra Small: `xs:text-[10px]`
  - Small и выше: `sm:text-xs`

- **Border radius**:
  - Mobile: `rounded-lg`
  - Extra Small: `xs:rounded-xl`
  - Small и выше: `sm:rounded-2xl`

### 4. Доходы/Расходы информация

#### Улучшения
- **Минимальная ширина для контейнеров**: `min-w-0` для предотвращения overflow текста
- **Truncate для больших чисел**: Текст обрезается вместо переполнения
- **Адаптивные размеры иконок**:
  - Icon: `text-xs sm:text-base` вместо `text-sm sm:text-base`
  - Более плавный переход между размерами

## Использованные Tailwind breakpoints

```
'xs': '475px',   // Extra small devices (phablets)
'sm': '640px',   // Small devices (tablets landscape)
'md': '768px',   // Medium devices (tablets portrait)
'lg': '1024px',  // Large devices (desktops)
'xl': '1280px',  // Extra large devices
'2xl': '1536px'  // Ultra wide screens
```

## Результаты

### ✅ Мобильная версия
- Все элементы правильно отображаются на экранах < 480px
- Горизонтальный скролл для вкладок работает плавно
- Текст не переполняет контейнеры
- Кнопки имеют достаточный размер для тача (44px minimum)

### ✅ Планшетная версия
- Оптимизировано для экранов 480px-768px
- Хороший баланс между размерами элементов и доступным пространством
- Вкладки хорошо читаются без необходимости скролла на большинстве планшетов

### ✅ Desktop версия
- Полное отображение всех элементов
- Эмодзи в вкладках добавляют визуальный интерес
- Hover эффекты работают правильно

## Testing

Для проверки адаптивности используйте DevTools браузера:
1. Откройте Developer Tools (F12)
2. Включите Device Toolbar (Ctrl+Shift+M)
3. Протестируйте на разных размерах экранов:
   - iPhone SE (375px)
   - iPhone 12 (390px)
   - iPhone 14 Pro Max (430px)
   - iPad Mini (768px)
   - iPad Pro (1024px)
