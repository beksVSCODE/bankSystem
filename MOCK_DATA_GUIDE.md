# Переключение между Supabase и Mock Data

## Текущий статус
✅ **Проект настроен на использование MOCK DATA**

## Как это работает?

### Mock Mode (Текущий режим)
- **Данные:** Хранятся в памяти браузера (in-memory)
- **Файлы:** `src/services/mockService.ts`, `src/mock/`
- **Сервис:** Вся логика работает локально
- **Персистентность:** Данные не сохраняются после перезагрузки страницы
- **Скорость:** Моментальный ответ (без задержек сети)

### Supabase Mode
- **Данные:** Хранятся в удаленной базе данных Supabase
- **Файлы:** `src/services/supabaseService.ts`
- **Сервис:** Запросы идут на backend
- **Персистентность:** Все данные сохраняются автоматически
- **Скорость:** Зависит от интернета и сервера

---

## Как переключиться?

### 1️⃣ Для использования Mock Data (ТЕКУЩЕЕ)

Откройте файл `src/hooks/useSupabase.ts` и убедитесь, что там:

```typescript
import { accountService, transactionService, notificationService, userService } 
  from '@/services/mockService';  // ← ВОТ ЭТА СТРОКА
```

### 2️⃣ Для использования Supabase

Откройте файл `src/hooks/useSupabase.ts` и измените:

```typescript
// ❌ Замените эту строку:
import { accountService, transactionService, notificationService, userService } 
  from '@/services/mockService';

// ✅ На эту:
import { accountService, transactionService, notificationService, userService } 
  from '@/services/supabaseService';
```

### 3️⃣ Перезагрузите dev-сервер

```bash
npm run dev
# или
bun run dev
```

---

## Структура Mock Data

```
src/
├── mock/
│   ├── authStore.ts           # Аутентификация (всегда mock)
│   ├── data.ts                # Sample данные (пользователь, счета, карты, транзакции)
│   ├── supabaseFinancialStore.ts  # Zustand хранилище для финансовых операций
│   └── types.ts               # TypeScript типы
│
├── services/
│   ├── mockService.ts         # ← ИСПОЛЬЗУЕТСЯ СЕЙЧАС (локальный сервис)
│   └── supabaseService.ts     # (используется при переключении на Supabase)
│
└── hooks/
    └── useSupabase.ts         # React hooks для работы с сервисом
```

---

## Что входит в Mock Data?

### 👤 Пользователь
- Имя: Александр Петров
- Email: a.petrov@mail.ru
- Телефон: +7 (999) 123-45-67

### 💳 Счета
1. **Основной счёт** (RUB) - 245,890 ₽
2. **Накопительный счёт** (RUB) - 500,000 ₽
3. **USD счёт** - 5,000 $
4. **EUR счёт** - 3,000 €

### 💰 Карты
- Дебетовые карты для основного счета
- Виртуальные карты для безопасных платежей

### 📊 Транзакции
- 50+ примеров разных категорий
- Доходы, расходы, переводы, платежи

### 🔔 Уведомления
- Примеры различных типов уведомлений

---

## Важные отличия при использовании Mock

| Функция | Mock | Суpabase |
|---------|------|----------|
| Создание счета | ✅ Работает (в памяти) | ✅ Сохраняется в БД |
| Создание транзакции | ✅ Работает (в памяти) | ✅ Сохраняется в БД |
| Перезагрузка страницы | ❌ Данные обнуляются | ✅ Данные остаются |
| Скорость ответа | ⚡ Мгновенно | ⚡ ~200-500ms |
| Разработка | ✅ Удобно | ✅ Реалистично |

---

## Ошибки при переключении?

### Если видите ошибки импорта
```
Cannot find module '@/services/mockService'
```
✅ Убедитесь, что файл `src/services/mockService.ts` существует

### Если Supabase не работает
```
Error: Supabase client is not initialized
```
✅ Проверьте переменные окружения в `.env.local`:
```
VITE_SUPABASE_URL=your_url
VITE_SUPABASE_ANON_KEY=your_key
```

### Если данные не обновляются
✅ Очистите localStorage:
```javascript
localStorage.clear()
```
Затем перезагрузите страницу.

---

## Для быстрой разработки

**Рекомендуется использовать Mock Mode** потому что:
- ✅ Не нужно ждать ответа сервера
- ✅ Можно разрабатывать без интернета
- ✅ Не нужны переменные окружения
- ✅ Тестирование UI/UX намного быстрее

**Переходите на Supabase когда:**
- Нужна персистентность данных
- Нужна синхронизация между устройствами
- Готовы к production

---

## Полезные команды

```bash
# Разработка с Mock Data
npm run dev

# Сборка для production
npm run build

# Запуск тестов
npm run test

# Проверка типов
npm run type-check
```

---

## Дополнительная информация

- 📖 [Документация Mock Data](../mock/)
- 🔐 [Документация Supabase](../SUPABASE_SETUP.md)
- 📋 [Быстрый старт](../docs/)
