# PWA (Progressive Web App) Setup

## ✅ Что сделано

### 1. Манифест приложения
- **Файл**: `public/manifest.json`
- **Настройки**:
  - Название: FinSim Dashboard
  - Тема: #0050B3 (синий)
  - Режим отображения: standalone (как нативное приложение)
  - Поддержка иконок: 8 размеров (72px - 512px)
  - Shortcuts: быстрый доступ к счетам и транзакциям

### 2. Service Worker
- **Файл**: `public/sw.js`
- **Стратегия кэширования**: Network First (сначала сеть, затем кэш)
- **Возможности**:
  - Оффлайн доступ к приложению
  - Автоматическое обновление кэша
  - Быстрая загрузка повторных посещений

### 3. Иконки
- **Папка**: `public/icons/`
- **Форматы**: SVG + PNG (8 размеров)
- **Временно**: используются копии SVG (замените на настоящие PNG)

### 4. HTML мета-теги
- **Файл**: `index.html`
- Добавлены PWA мета-теги для iOS и Android
- Подключен манифест
- Регистрация Service Worker

### 5. Install Prompt компонент
- **Файл**: `src/components/PWAInstallPrompt.tsx`
- Кнопка "Установить приложение" (появляется автоматически)
- Можно отклонить (сохраняется в localStorage)
- Адаптивный дизайн (снизу на мобильных, справа на десктопе)

## 🚀 Как проверить PWA

### На localhost (для разработки):

1. **Chrome DevTools**:
   - Откройте DevTools (F12)
   - Перейдите в Application → Manifest
   - Проверьте что манифест загружен
   - В Service Workers проверьте регистрацию SW

2. **Lighthouse**:
   - DevTools → Lighthouse
   - Выберите "Progressive Web App"
   - Запустите аудит
   - Смотрите результаты и рекомендации

3. **Установка на десктопе**:
   - В Chrome адресная строка покажет иконку установки
   - Или меню → "Установить FinSim Dashboard"

### На production (после деплоя):

1. **Требования**:
   - HTTPS обязательно (не работает на HTTP)
   - Все иконки должны быть доступны
   - Service Worker должен регистрироваться

2. **Установка на мобильном**:
   - Android Chrome: меню → "Установить приложение"
   - iOS Safari: кнопка "Поделиться" → "На экран Домой"

3. **Автоматическое предложение**:
   - Компонент PWAInstallPrompt покажет кнопку установки
   - Работает только на production с HTTPS

## 📝 Что нужно улучшить

### Высокий приоритет:
1. **Иконки PNG**:
   ```bash
   # Используйте pwa-asset-generator
   npm install -g pwa-asset-generator
   pwa-asset-generator public/icons/icon.svg public/icons
   ```
   
   Или онлайн: https://www.pwabuilder.com/

2. **Обновите Service Worker стратегию** (опционально):
   - Для Supabase данных: Network Only (всегда свежие данные)
   - Для статики: Cache First (максимально быстро)
   - Для API: Stale While Revalidate (быстро + актуально)

3. **Push уведомления** (будущее):
   - Добавить в Service Worker обработку push
   - Настроить на бэкенде отправку уведомлений

### Средний приоритет:
1. **Offline страница**:
   - Создать красивую страницу для offline режима
   - Показывать когда нет сети

2. **Update prompt**:
   - Уведомлять пользователя о новой версии
   - Кнопка "Обновить приложение"

3. **Share Target API**:
   - Позволить делиться файлами/текстом в приложение
   - Добавить в manifest.json

## 🔧 Команды

### Проверка PWA готовности:
```bash
npm run build
npm run preview
# Откройте http://localhost:4173
# DevTools → Lighthouse → PWA audit
```

### Генерация иконок:
```bash
# Установите генератор
npm install -g pwa-asset-generator

# Создайте PNG из SVG
pwa-asset-generator public/icons/icon.svg public/icons --icon-only

# Или используйте свой логотип
pwa-asset-generator path/to/logo.png public/icons
```

### Тестирование Service Worker:
```bash
# 1. Соберите проект
npm run build

# 2. Запустите production preview
npm run preview

# 3. Откройте DevTools → Application → Service Workers
# Должен быть зарегистрирован и активирован
```

## 📱 Поддержка браузеров

| Браузер | Версия | Установка | Push | Offline |
|---------|--------|-----------|------|---------|
| Chrome | 40+ | ✅ | ✅ | ✅ |
| Firefox | 44+ | ✅ | ✅ | ✅ |
| Safari | 11.1+ | ⚠️ | ❌ | ✅ |
| Edge | 17+ | ✅ | ✅ | ✅ |
| Opera | 32+ | ✅ | ✅ | ✅ |
| Samsung | 4+ | ✅ | ✅ | ✅ |

⚠️ Safari iOS: добавление через "Поделиться" → "На экран Домой"

## 🎯 Критерии качества PWA

Google Lighthouse проверяет:
- ✅ Регистрирует Service Worker
- ✅ Отвечает 200 когда offline
- ✅ Имеет manifest.json
- ✅ Настроена тема для строки состояния
- ✅ Имеет иконки 192px и 512px
- ✅ Работает на HTTPS
- ✅ Перенаправляет HTTP на HTTPS
- ✅ Настроен viewport meta tag
- ✅ Не использует конфликтующие библиотеки

Текущий статус после внедрения: **~85-95/100** (на production с настоящими иконками)

## 🔗 Полезные ссылки

- [PWA Builder](https://www.pwabuilder.com/) - генератор PWA манифеста
- [Workbox](https://developers.google.com/web/tools/workbox) - инструменты для Service Worker
- [Web.dev PWA Guide](https://web.dev/progressive-web-apps/)
- [MDN PWA Docs](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
